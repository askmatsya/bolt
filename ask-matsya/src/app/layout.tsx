import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Ask Matsya - AI Voice Assistant for Ethnic Products',
  description: 'Discover authentic Indian ethnic products with AI-powered voice assistant. Built with Speechmatics and Next.js.',
  keywords: 'AI, voice assistant, Indian ethnic products, Speechmatics, Next.js',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className} suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}